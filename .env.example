# ============================================================
# ScholarGraph3D Environment Variables
# ============================================================
# Copy this file to .env and fill in the values.
# Required values are marked with (REQUIRED).
# ============================================================

# ==================== Supabase (REQUIRED) ====================
# Get from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
SUPABASE_URL=https://your-project.supabase.co          # (REQUIRED)
SUPABASE_KEY=your-anon-key                              # (REQUIRED) anon/public key
SUPABASE_JWT_SECRET=your-jwt-secret                     # (REQUIRED) Settings → API → JWT Secret
DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres  # (REQUIRED)

# ==================== Semantic Scholar ====================
# Get from: https://www.semanticscholar.org/product/api#api-key
S2_API_KEY=                                             # (RECOMMENDED) for 1 RPS authenticated access

# ==================== OpenAlex (REQUIRED) ====================
# Get from: https://docs.openalex.org/how-to-use-the-api/api-key
OA_API_KEY=your-openalex-api-key                        # (REQUIRED) for 100K credits/day
OA_EMAIL=your@email.com                                 # (RECOMMENDED) for polite pool access

# ==================== Redis (Upstash) ====================
# Get from: https://console.upstash.com/ → Create Database → REST URL
REDIS_URL=                                              # (OPTIONAL) for search result caching

# ==================== LLM Providers (Phase 2) ====================
# Users bring their own keys via frontend. These are server-side fallbacks (optional).
OPENAI_API_KEY=                                         # (OPTIONAL) server fallback
ANTHROPIC_API_KEY=                                      # (OPTIONAL) server fallback
GOOGLE_API_KEY=                                         # (OPTIONAL) server fallback
GROQ_API_KEY=                                           # (OPTIONAL) server fallback
DEFAULT_LLM_PROVIDER=groq                               # Default: groq

# ==================== Email — Resend (Phase 3) ====================
# Get from: https://resend.com/api-keys
RESEND_API_KEY=                                         # (OPTIONAL) for watch query email notifications
NOTIFICATION_FROM_EMAIL=notifications@scholargraph3d.com

# ==================== Watch Queries (Phase 3) ====================
WATCH_CRON_SECRET=                                      # (AUTO-GENERATED) secret for cron endpoint auth

# ==================== Deployment ====================
ENVIRONMENT=development                                 # development | staging | production
CORS_ORIGINS=http://localhost:3000                      # Comma-separated allowed origins

# ==================== Frontend (Vercel) ====================
NEXT_PUBLIC_API_URL=http://localhost:8000                # Backend API URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
